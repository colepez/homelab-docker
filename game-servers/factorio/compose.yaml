# Migrated to oraclevm 2026-09-06 (removes the oraclevm<->dockerz Tailscale
# relay hop entirely -- server now runs directly on the public-facing host).
# Deliberately commented out so this stack deploys with nothing running.
services: {}

networks:
  edge:
    external: true
